# 账号系统 Backlog

## Summary

账号系统不再被设计为“替代同步码”的单一同步方案，而是产品的身份、空间管理、全局偏好和会员权益层。

新的长期模型：

```text
Account
  -> Profile
  -> Global Preferences
  -> Billing / Entitlements
  -> Home Spaces
       -> Home Document
       -> Sync Codes
       -> Devices
```

在这个模型下：

- 账号登录是上级身份层。
- 同步码是某个首页空间的访问凭证。
- 一个账号可以挂载一个或多个首页空间。
- 每个首页空间可以有一个或多个同步码。
- 登录账号后，用户可以选择和切换账号下的首页空间。
- 账号负责同步展示风格、系统设置、语言、字体、会员状态等全局信息。
- 同步码继续保留低摩擦跨设备同步价值，不和账号登录互斥。

## Product Position

账号系统承担三类长期价值：

- 身份价值：让用户在不同设备上识别自己，管理自己的首页空间、同步码和设备。
- 配置价值：同步语言、主题、字体、默认搜索引擎、组件默认行为等全局偏好。
- 商业价值：作为 VIP 付费、空间数量、组件权限、历史版本、AI 额度等权益认证入口。

## Requirement Table

| 分类 | 需求 | 用户价值 | 实现要点 | 优先级 | 阶段建议 |
|---|---|---|---|---|---|
| 账号基础 | 邮箱 Magic Link 登录 | 低摩擦建立身份 | Supabase Auth，持久 session，GitHub Pages redirect | P0 | Phase 1.5 |
| 账号基础 | 登出 | 明确断开账号状态 | 清理 auth session，不删除本地首页 | P0 | Phase 1.5 |
| 账号基础 | 当前账号状态展示 | 用户知道是否登录 | 设置页账号卡片，首页状态区轻量展示 | P0 | Phase 1.5 |
| 账号基础 | 用户资料 | 后续展示名称、头像、语言 | `profiles` 或 `account_preferences` 保存基础信息 | P1 | Phase 1.6 |
| 首页空间 | 账号下挂多个首页空间 | 支持工作、个人、学习等多套首页 | 新增 `home_spaces` 索引表 | P0 | Phase 1.5 |
| 首页空间 | 首页空间切换 | 登录后选择当前首页 | 设置页空间列表和切换按钮 | P0 | Phase 1.5 |
| 首页空间 | 默认首页空间 | 打开首页自动使用常用空间 | 账号偏好保存 `default_space_id` | P1 | Phase 1.6 |
| 首页空间 | 创建/重命名/删除空间 | 多首页管理能力 | 删除需二次确认，优先软删除 | P1 | Phase 1.6 |
| 首页空间 | 空间排序 | 常用空间优先显示 | `order` 字段或 `last_used_at` | P2 | Phase 1.7 |
| 同步码管理 | 将当前匿名同步码认领到账号 | 平滑迁移已有用户 | `sync_spaces.owner_id` 或关联表绑定账号 | P0 | Phase 1.5 |
| 同步码管理 | 账号下查看同步码/空间列表 | 用户知道自己有哪些首页入口 | 只展示名称、状态、时间，不展示 secret 明文 | P0 | Phase 1.5 |
| 同步码管理 | 为空间创建新同步码 | 新设备继续低摩擦接入 | 复用现有同步码生成能力 | P1 | Phase 1.6 |
| 同步码管理 | 废弃单个同步码 | 同步码泄露后止损 | 设置 `revoked_at` | P0 | Phase 1.5 |
| 同步码管理 | 多同步码绑定同一空间 | 同一首页可发给多设备 | 需要同步凭证表或拆分 token 表 | P2 | Phase 1.7 |
| 设备管理 | 展示已绑定设备/浏览器 | 用户理解哪里在同步 | 本地生成 device id，记录 `last_seen_at` | P1 | Phase 1.6 |
| 设备管理 | 移除设备 | 控制同步入口 | 删除 device binding 或 revoke token | P2 | Phase 1.7 |
| 全局设置 | 同步语言 | 换设备后语言一致 | account preferences 保存 `locale` | P1 | Phase 1.6 |
| 全局设置 | 同步字体 | 保持阅读与展示偏好 | font family、size、density | P1 | Phase 1.6 |
| 全局设置 | 同步主题风格 | 多空间共享审美偏好 | accent、density、theme mode、背景策略 | P1 | Phase 1.6 |
| 全局设置 | 默认搜索引擎 | 用户个性化搜索入口 | DuckDuckGo、Google、Bing、Baidu 等 | P1 | Phase 1.6 |
| 全局设置 | 组件默认配置 | 新空间复用偏好 | widget registry 默认 config | P2 | Phase 1.7 |
| 安全 | RLS 隔离 | 防止越权读写 | 所有账号表基于 `auth.uid()` policy | P0 | Phase 1.5 |
| 安全 | 覆盖/删除二次确认 | 防止误操作 | 覆盖云端、删除空间、废弃码均确认 | P0 | Phase 1.5 |
| 安全 | 操作审计日志 | 出问题可追踪 | 记录登录、认领、废弃、覆盖、切换 | P1 | Phase 1.6 |
| 安全 | 数据导出 | 用户能迁移自己的配置 | 导出账号下空间索引和首页 JSON | P1 | Phase 1.6 |
| 安全 | 账号删除 | 合规和用户控制 | 删除账号数据、空间、偏好和绑定 | P2 | Phase 1.8 |
| 付费权益 | VIP 状态认证 | 付费功能入口 | Stripe customer 和 entitlement 关联账号 | P0 | Phase 2 |
| 付费权益 | 首页空间数量限制 | 免费/付费分层 | free 限制空间数，VIP 增加额度 | P1 | Phase 2 |
| 付费权益 | 高级组件权限 | 组件商业化 | AI 额度、项目管理、股票、实时比分等 | P1 | Phase 2 |
| 付费权益 | 高级主题/图片能力 | 明显的付费价值 | 高级主题、Supabase Storage 或 R2 图片 | P1 | Phase 2 |
| 付费权益 | 历史版本和回滚 | 高价值安全能力 | revision snapshot，可恢复旧版本 | P1 | Phase 2 |
| 增长 | 模板库收藏 | 快速创建不同首页 | 官方模板、用户收藏模板 | P1 | Phase 1.7 |
| 增长 | 分享只读首页 | 带来传播入口 | share link 默认只读，不暴露编辑权 | P1 | Phase 1.8 |
| 增长 | 公开主页 | 形成展示/分发能力 | public slug，后续再考虑 SEO | P2 | Phase 2 |
| 增长 | 团队/家庭共享 | 多人维护同一首页 | membership、role、invite | P2 | Phase 2+ |

## Phase 1 Extraction

Phase 1 只抽取账号系统中能支撑 MVP 的部分，不引入付费、不做团队、不做复杂设备控制。

Phase 1.5 应覆盖：

- 账号登录：
  - 邮箱 Magic Link。
  - 登录状态展示。
  - 登出。
  - 刷新后保持 session。
- 账号全局设置骨架：
  - 先保存最小偏好字段，例如 `locale`、`defaultSpaceId`、`themePreference`。
  - 不要求立刻做完整主题和字体 UI。
- 账号首页空间索引：
  - 一个账号可以拥有多个 `home_spaces`。
  - 每个 `home_space` 先关联一个现有 `sync_spaces`。
  - 首页内容本身继续沿用现有同步码密文同步机制。
- 同步码认领：
  - 登录后可以把当前本机绑定的同步码挂到账户下。
  - 认领后，该同步码仍可继续被未登录设备使用。
  - 账号只是管理和索引它，不改变同步码的低摩擦能力。
- 首页空间切换：
  - 设置页显示账号下已认领的首页空间。
  - 用户可选择某个空间并把其同步码绑定到当前浏览器。
  - 切换空间必须提示会拉取该空间首页并覆盖当前本地显示。
- 安全边界：
  - 账号表启用 RLS。
  - 用户只能查看和管理自己认领的空间。
  - 同步码 secret 不应以明文长期展示在账号空间列表中。

Phase 1 暂不做：

- Stripe 或会员支付。
- 团队共享。
- 多同步码对应同一空间的完整凭证管理。
- 公开主页。
- 高级组件权益。
- 历史版本回滚。
- 图片存储和高级主题上传。

## Updated Phase 1.5 Breakdown

### Phase 1.5.0：账号模型与数据库计划

- 明确账号是上级身份层，同步码是首页空间凭证。
- 设计 `profiles`、`account_preferences`、`home_spaces` 的最小表结构。
- 决定 `home_spaces` 如何关联当前 `sync_spaces`。
- 写清楚 RLS policy 和验收 SQL。

### Phase 1.5.1：Supabase Auth 基础登录

- 调整 Supabase browser client：
  - 开启 session 持久化。
  - 开启 token 自动刷新。
  - 开启 URL session 检测。
- 设置页账号区支持：
  - 输入邮箱。
  - 发送 Magic Link。
  - 显示当前账号。
  - 登出。
- 不写首页空间数据。

### Phase 1.5.2：账号资料与全局设置骨架

- 创建最小账号资料和偏好表。
- 登录后读取或初始化账号偏好。
- 先展示语言、主题、默认空间等占位状态。
- 不在本阶段实现完整主题/字体配置 UI。

### Phase 1.5.3：账号首页空间索引与同步码认领

- 新增账号下的 `home_spaces`。
- 登录后可以把当前本机绑定的同步码认领到账户。
- 认领时用户需要填写或确认空间名称。
- 已认领空间在设置页账号区域中展示。

### Phase 1.5.4：首页空间切换

- 设置页展示账号下空间列表。
- 点击某个空间可切换当前浏览器绑定的同步码。
- 切换前二次确认。
- 切换后执行现有同步码拉取流程。
- 当前空间状态写入本地缓存和账号偏好。

### Phase 1.5.5：安全回归与线上验证

- 验证未登录用户不能读取账号表。
- 验证账号 A 不能读取账号 B 的空间列表。
- 验证登录前后同步码创建、绑定、上传、拉取、废弃仍正常。
- 验证 GitHub Pages 登录回调。
- 验证空间切换不会静默覆盖本地数据。

## Later Phases

### Phase 1.6

- 创建/重命名/删除首页空间。
- 为已存在空间生成新的同步码。
- 完整同步语言、字体、默认搜索引擎、主题偏好。
- 设备列表 v1。
- 数据导出 v1。

### Phase 1.7

- 多同步码凭证管理。
- 模板库收藏。
- 组件默认配置。
- 操作审计日志。

### Phase 1.8

- 只读分享链接。
- 账号删除。
- 更完整的设备移除。

### Phase 2

- Stripe 会员。
- VIP 权益认证。
- 高级组件。
- 空间数量限制。
- 历史版本和回滚。
- 公开主页和团队协作。

## Risk Notes

- 最大风险从“账号同步覆盖本地首页”变为“空间切换覆盖当前本地首页”。所有空间切换和拉取都必须二次确认。
- RLS 配置错误可能导致账号空间越权访问，必须单独验收。
- 同步码认领不能泄露同步码 secret；账号空间列表默认只展示空间元数据。
- Supabase Auth session 配置变化可能影响现有同步码 RPC，需要完整回归。
- GitHub Pages 是静态部署，需要提前验证 Auth redirect URL、base path 和 callback 行为。

## Recommended Position

Phase 1.5 可以开始，但不应直接实现“账号自动同步替代同步码”。更稳的落地方式是：先让账号管理和切换多个同步码首页空间，再逐步扩展全局设置、设备管理和付费权益。
