# Phase 1.9 Implementation Plan：页面布局与导入需求集

## 阶段定位

Phase 1.9 不再只保留“浏览器收藏/标签导入”一个大块，而是拆成两个连续部分：

- Phase 1.9A：前端页面布局和 UI/UX 优化。先降低现有功能的编辑、创建和配置成本，让首页和设置页更适合日常重复使用。
- Phase 1.9B：浏览器收藏/标签导入需求集。在 UI/UX 基础收口后，再专门设计权限边界、大批量导入、预览确认和回滚策略。

Phase 1.9A 原则上不新增 Supabase 表、不新增 Storage bucket、不改变 `HomeDocumentV2` 的核心同步协议。Phase 1.9B 在需求设计阶段优先产出方案和边界，不默认承诺浏览器扩展或云端导入服务。

## Phase 1.9A：前端页面布局和 UI/UX 优化

### Phase 1.9.0：设置页信息架构收口

目标是减少设置页重复信息，让账号、空间和高级操作更像一个稳定后台，而不是多个历史阶段功能的堆叠。

任务范围：

- 将“账号托管状态”合并到“账号”栏目中，保留登录状态、账号托管可用性、Supabase 未配置状态和必要的禁用原因。
- 将“离线同步码与恢复”和“配置文件”合并到“高级操作”栏，统一承载低频、恢复类、导入导出类动作。
- 保留未登录、本地模式、同步码模式、账号托管模式下的关键状态提示，但避免同一状态在多个面板重复出现。

影响：

- 前端：主要调整设置页组件组合、文案和状态展示优先级。
- 后端：无新增 SQL，无新增 RPC。
- 架构：不改变同步码、账号托管空间或账号偏好模型。

风险：

- 不能因为合并面板而隐藏“当前是否账号托管”“是否已登录”“Supabase 是否未配置”等关键诊断信息。
- 高级操作区需要明确危险操作和普通导出操作的层级，避免误操作。

### Phase 1.9.1：首页空间创建流程弹窗化

目标是让“使用当前首页创建空间”和“从模板创建新空间”两个动作真正成为可完成的流程，而不是在设置页里直接铺开所有表单和说明。

任务范围：

- “使用当前首页创建空间”点击后弹出对话框：
  - 支持输入空间名称。
  - 支持使用默认名称。
  - 支持取消和确认。
  - 确认后复用当前首页文档创建账号托管空间。
- “从模板创建新空间”点击后弹出对话框：
  - 包含必要说明。
  - 支持模板选择。
  - 支持取消和确认。
  - 确认后使用模板生成普通 `HomeDocumentV2`，再进入现有账号托管空间创建链路。
- 设置页只保留两个入口按钮和简短状态，不直接展开完整创建细节。

影响：

- 前端：新增或复用设置页 modal/dialog 状态，复用模板库和空间创建逻辑。
- 后端：无新增 SQL，继续使用现有账号托管空间创建流程。
- 架构：不引入新的空间类型；模板结果仍是普通首页文档。

风险：

- 当前首页创建和模板创建都要保持失败回滚语义，不能创建半个空间或错误切换当前空间。
- 默认空间、当前空间、创建后是否激活等行为必须和现有首页空间管理一致。

### Phase 1.9.2：Banner/背景设置面板布局优化

目标是解决图片设置区元素挤压、按钮被挡和状态标识不齐的问题。

任务范围：

- Banner 和背景两块保持上下排布，每块独占横向空间。
- 每块左上角显示“Banner”或“背景”标签。
- 预览图占满一行。
- 上传、清除、外链 URL、遮罩强度等剩余元素置于下方，占满一行并可响应式换行。
- “未设置”等状态标识与“上传”“清除”按钮保持同一操作高度。

影响：

- 前端：主要调整 `ThemeImagePanel` 及相关 CSS。
- 后端：无新增 SQL，无新增 Storage policy。
- 架构：继续使用 Phase 1.8.1 的图片 asset 和遮罩强度字段。

风险：

- 长 URL、未登录禁用态、上传错误、窄屏和紧凑密度都要回归。
- 预览图尺寸需要稳定，避免图片加载前后导致设置页跳动。

### Phase 1.9.3：首页网站收集区编辑入口优化

目标是让新增网址和新增分组更低成本，同时保持首页默认浏览状态干净。

桌面端方案：

- 顶层“未分组”栏最前方的“增加网址”按钮改为圆形，直径与网站标签高度一致。
- 默认状态下按钮弱化或隐藏，网站列表靠左排列。
- 当鼠标 hover 到该栏最前方的触发区域，或键盘 focus 进入该区域时，网站列表向右滑动，显示圆形“增加网址”按钮。
- 页面底部“新增分组”按钮也改为同规格圆形按钮，默认弱化或隐藏，hover/focus 对应区域时显示。
- 动画要尊重 `prefers-reduced-motion`，关闭动效时直接显示/隐藏，不做滑动。

触屏端方案：

- 触屏设备没有 hover，新增网址和新增分组不能完全隐藏。
- 使用输入能力而不是只用屏幕宽度判断：
  - `(hover: hover) and (pointer: fine)`：桌面细指针设备使用 hover/focus reveal。
  - `(hover: none), (pointer: coarse)`：触屏或粗指针设备保持圆形按钮可见。
- 触屏端不做“列表先靠左、触发后再挤出按钮”的滑入逻辑；入口应作为稳定 inline 控件出现，避免用户不知道哪里可以点。
- 触屏按钮点击目标按至少 44px 级别设计，并提供明确的 `aria-label`。
- 不把长按、横向滑动露出按钮作为唯一入口；这类手势可作为增强，但不能承担主路径。

影响：

- 前端：调整首页网站列表、未分组区和新增分组入口的 DOM/CSS 结构。
- 后端：无新增 SQL。
- 架构：不改变 group/site 数据结构，只改变编辑入口的可发现性和布局。

风险：

- 网站列表滑动不能造成布局抖动、文本遮挡或移动端横向溢出。
- 需要同时覆盖鼠标、触屏、键盘和 reduced motion。
- 首页直编按钮不能遮挡网站标签点击区域。

### Phase 1.9.4：UI/UX 回归与可访问性收口

目标是给 Phase 1.9A 的跨页面布局调整做集中回归。

任务范围：

- 检查首页、设置页、模板弹窗、空间创建弹窗、图片设置面板在桌面、平板和移动端的布局。
- 检查浅色/深色、6 个主题 preset、背景图存在、紧凑密度、serif/mono 字体下的按钮和文本溢出。
- 检查键盘 Tab 顺序、焦点样式、触屏目标尺寸、hover/focus 状态和 disabled 状态。
- 检查 Supabase 未配置、未登录、上传失败、空间创建失败等错误提示是否仍然可读。

影响：

- 前端：CSS 和局部组件 polish。
- 后端：无新增。
- 架构：无新增。

风险：

- 改动范围横跨首页和设置页，必须避免局部修复引入新的重叠或按钮不可达问题。

### 已落地能力

Phase 1.9.0 到 Phase 1.9.4 已完成实现。本阶段没有新增 Supabase 表、RPC、Storage bucket 或首页文档 schema。

- 设置页撤掉独立“状态总览”，账号栏新增当前首页托管/同步状态摘要，账号托管、普通同步码、本地首页、同步暂停、同步冲突和 Supabase 未配置状态统一在账号上下文中说明。
- 已登录用户的“离线同步码与恢复”合并进“高级操作”，配置文件的 JSON 导入导出、恢复上一次重置前页面、清空恢复默认和数据包导出也统一进入“高级操作”。
- 账号卡片中的退出登录按钮右对齐，账号说明不再保留“首页内容仍由同步码管理”等过期文案。
- “首页空间”面板不再直接展开模板库和创建表单，只保留“使用当前首页创建空间”和“从模板创建新空间”两个入口。
- 使用当前首页创建空间改为 modal：支持输入名称、使用默认名称、取消和确认。
- 从模板创建新空间改为两步 modal：先选择模板并确认，再输入空间名称并确认创建；模板应用后仍生成普通 `HomeDocumentV2` 并复用账号托管空间创建链路。
- Banner/背景图片面板调整为标题、整行预览、整行控制区；未设置图片时预览占位高度压缩为 40px，已设置图片时保持正常比例；按钮+URL 占满一行，遮罩强度下移到第二行并占满整行。
- 首页未分组新增网站入口和底部新增分组入口改为紧凑圆形按钮，直径约为网站标签高度的一半。
- 桌面细指针设备使用 `(hover: hover) and (pointer: fine)` 做 hover/focus reveal；默认状态弱化隐藏入口，未分组网站列表靠左排列，hover/focus 后列表让出按钮空间。
- 触屏或粗指针设备使用 `(hover: none), (pointer: coarse)` 保持新增入口可见，不依赖 hover。
- 为新增入口、模板选择卡片和 modal 操作补齐焦点样式、触屏目标尺寸和 `prefers-reduced-motion` 处理。

### 验证记录

- `npm run typecheck` 通过。
- `npm run lint` 通过。
- `npm run build` 通过。
- 本地 dev server `http://127.0.0.1:3000/` 可启动；桌面视口下首页新增网站和新增分组按钮为紧凑圆形按钮，默认隐藏且无横向溢出。
- 设置页检查：独立状态总览不存在；未设置 Banner/背景预览高度为 40px；遮罩控制位于按钮+URL 下方且占满整行；无横向溢出。
- 登录态下账号托管空间创建 modal 仍需人工使用真实账号回归创建成功、失败提示和创建后切换行为。

## Phase 1.9B：浏览器收藏/标签导入需求集

### Phase 1.9.5：收藏/标签导入需求设计

目标是先明确浏览器能力边界，避免把普通网页不能做到的能力设计成主路径。

详细设计见 [`Phase1_9_5_BookmarkImportDesign.md`](Phase1_9_5_BookmarkImportDesign.md)。该文档是 Phase 1.9B 的设计基线，本阶段只产出需求设计和技术方案，不实现导入 UI、解析器或数据写入逻辑。

关键结论：

- 普通网页不能直接读取用户浏览器收藏夹。
- 普通网页也不能直接枚举当前浏览器已打开标签页。
- 可行入口应从用户主动提供数据开始：书签 HTML 文件、粘贴 URL 列表，或后续单独开发浏览器扩展。
- MVP 推荐优先考虑书签 HTML 文件导入和 URL 列表粘贴，浏览器扩展作为后续能力评估。
- 导入过程应先生成本地草稿，完成预览、去重、分组映射和用户确认后，再写入当前 `HomeDocumentV2.groups[].sites`。

已产出：

- 导入入口对比：书签 HTML、URL 列表粘贴、浏览器扩展。
- 导入数据模型草案：原始条目、规范化 URL、标题、来源文件/来源批次、候选分组、去重状态。
- 安全边界：不上传用户原始收藏文件，除非用户明确确认；导入前本地解析和预览优先。
- MVP 选择建议：优先考虑书签 HTML 导入和 URL 列表粘贴，浏览器扩展作为后续能力。
- 前后端影响：Phase 1.9.5 不新增 Supabase 表、RPC、Storage bucket 或 `HomeDocumentV2` schema；确认导入后的内容继续沿用现有本地、同步码和账号托管同步机制。

### Phase 1.9.6：大批量导入体验设计

目标是让上千收藏也可控，避免“一键导入后首页爆炸”。

详细设计见 [`Phase1_9_6_BulkImportExperienceDesign.md`](Phase1_9_6_BulkImportExperienceDesign.md)。该文档承接 Phase 1.9.5 的权限边界和导入草稿模型，进一步定义大批量导入的向导、预览、分组映射、草稿恢复、撤销和性能策略。

关键结论：

- 本地解析后先进入导入草稿，不直接写入正式首页。
- 提供统计摘要：总数、可导入数、重复数、无效 URL 数、候选分组数。
- 提供去重策略：同 URL、同 host、同标题近似项的不同处理级别。
- 提供分组映射：保留书签文件夹、合并到现有分组、创建新分组、导入到未分组。
- 提供抽样预览、搜索过滤和分页预览；虚拟列表作为后续增强，不作为 MVP 硬依赖。
- 草稿可以进入 localStorage，但只保存解析后的最小草稿，不保存原始书签 HTML，默认 24 小时 TTL。
- 提供分批选择和确认，必要时支持撤销最近一次导入。

已产出：

- 5 步导入向导：来源、解析摘要、分组映射、条目预览、确认导入。
- 草稿保存和恢复策略：`homepage:bookmark-import-draft:v1`，只保留最近一个草稿，默认 24 小时 TTL。
- 最近一次导入撤销策略：`homepage:bookmark-import-undo:v1`，保存提交前快照和新增 group/site id。
- 大批量性能策略：10 MB 文件上限、5,000 条解析软边界、分页优先、Map/Set 去重和搜索 debounce。
- Phase 1.9.7 MVP 范围：书签 HTML、URL 列表粘贴、摘要、分组映射、分页预览、确认写入、最近一次导入撤销。

### Phase 1.9.7：导入 MVP 实现

Phase 1.9.7 基于 Phase 1.9.5 和 Phase 1.9.6 的设计结果，已选择“书签 HTML 文件导入”和“URL 列表粘贴”作为 MVP 入口。浏览器扩展和云端批处理仍暂不进入本阶段。

已落地能力：

- 设置页 `高级操作` 新增“收藏 / 链接导入”入口，支持新建导入、继续上次导入草稿、撤销最近一次导入。
- 书签 HTML 文件导入：
  - 用户主动选择浏览器导出的 bookmarks HTML。
  - 前端使用 `DOMParser` 本地解析 folder/title/url。
  - 原始书签 HTML 不上传，也不保存到 localStorage。
- URL 列表粘贴：
  - 支持普通 URL、无协议域名、`www.` 链接和 Markdown 链接。
  - 标题缺失时用 host 兜底生成网站名和 mark。
- 5 步导入向导：
  - 来源。
  - 解析摘要。
  - 分组映射。
  - 条目预览。
  - 确认导入。
- 去重与默认选择：
  - 相同 URL、当前首页已有 URL、同 host、批次内重复和无效 URL 会被标记。
  - 新链接和同 host 不同路径默认选中；当前完全重复、批次重复和无效 URL 默认不选。
- 分组映射：
  - 书签文件夹可创建新分组、合并到现有分组、导入未分组或跳过。
  - 未分组来源默认导入到 `UNGROUPED_GROUP_ID`。
- 大批量预览：
  - 支持搜索、状态过滤、分组过滤、分页、当前结果批量选择/取消、只保留新链接和恢复默认选择。
  - MVP 使用分页优先，不引入虚拟列表依赖。
- 草稿与回滚：
  - 导入草稿保存到 `homepage:bookmark-import-draft:v1`，默认 24 小时 TTL，只保存解析后的最小草稿。
  - 最近一次导入撤销记录保存到 `homepage:bookmark-import-undo:v1`，默认 24 小时 TTL。
  - 撤销按新增 group/site id 清理，若新建分组之后已有其他内容，会保留该分组。
- 写入路径：
  - 确认导入后生成普通 `HomeDocumentV2.groups[].sites` 变更。
  - 不新增 Supabase 表、RPC、Storage bucket 或首页文档 schema。
  - 当前首页若已绑定同步码或账号托管空间，后续继续沿用现有完整首页文档同步流程。

### Phase 1.9.7 验证记录

- 2026-06-23：`npm run typecheck` 通过。
- 2026-06-23：`npm run lint` 通过。
- 2026-06-23：`npm run build` 通过。
- 2026-06-23：`git diff --check` 通过。
- 2026-06-23：本地 dev server `http://127.0.0.1:3000/edit` 返回 200；当前已有 Next dev server 在该目录运行。

## 验收标准

- Phase 1.9A 文档和计划表明确拆分为页面/UI 优化与导入需求集。
- 设置页信息架构、首页空间创建、Banner/背景面板和网站收集区编辑入口各自有明确子阶段。
- 触屏设备的新增入口不依赖 hover，不隐藏关键动作。
- Phase 1.9B 明确普通网页不能直接读取浏览器收藏夹或标签页。
- 大批量导入方案已覆盖解析、去重、预览、分组映射、确认和回滚；Phase 1.9.7 已完成书签 HTML 和 URL 列表粘贴的 MVP 实现。
